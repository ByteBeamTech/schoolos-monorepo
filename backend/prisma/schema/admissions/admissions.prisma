model Admission {
  id           String          @id @default(cuid())
  status       AdmissionStatus @default(INQUIRY)
  firstName    String
  lastName     String
  academicYear String
  tenantId     String
  branchId     String
  createdAt    DateTime        @default(now())
}

